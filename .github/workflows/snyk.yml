name: Snyk Security Scan
on:
  push:
    branches:
      - main
    paths:
      - "apps/**"
      - "src/**"
      - "streamlit_app.py"
      - "requirements*.txt"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/snyk.yml"
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
jobs:
  snyk:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - name: Check Snyk token
        id: snyk_token
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: |
          sanitize_secret() {
            local var_name="$1"
            local raw="$2"
            local norm
            norm="$(printf '%s' "$raw" | tr '[:upper:]' '[:lower:]' | xargs)"
            case "$norm" in
              ""|"token"|"tokens"|"secret"|"secrets"|"changeme"|"change-me"|"change_me"|"replace-me"|"replace_me"|"placeholder"|"example"|"redacted")
                printf -v "$var_name" '%s' ""
                ;;
              *)
                printf -v "$var_name" '%s' "$raw"
                ;;
            esac
          }
          sanitize_secret SNYK_TOKEN "$SNYK_TOKEN"

          if [ -n "$SNYK_TOKEN" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Set up Node.js
        if: steps.snyk_token.outputs.available == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install Snyk CLI
        if: steps.snyk_token.outputs.available == 'true'
        run: npm install -g snyk
      - name: Run Snyk (all projects, fail on high)
        if: steps.snyk_token.outputs.available == 'true'
        run: snyk test --all-projects --severity-threshold=high
      - name: Skip (missing token)
        if: steps.snyk_token.outputs.available != 'true'
        run: |
          echo "SNYK_TOKEN not configured; skipping Snyk scan" >> $GITHUB_STEP_SUMMARY
          exit 0
