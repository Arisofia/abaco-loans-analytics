# AGENTS

- For changes to the web app under `apps/web`, install dependencies and run `npm run lint` from that directory.
- Keep generated build artifacts out of the repo; `.gradle/` and `build/` should stay ignored.
